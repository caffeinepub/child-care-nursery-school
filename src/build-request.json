{
  "kind": "build_request",
  "title": "Deploy the current draft to the live (mainnet) site",
  "priority": "normal",
  "requirements": [],
  "constraints": [
    "Deployment must target the Internet Computer mainnet (production) and not local replica.",
    "Do not modify any files under frontend/src/components/ui or any path listed as immutable in SYSTEM_CONTEXT.",
    "Backend must remain single-actor Motoko (all logic in backend/main.mo). Only add backend/migration.mo if an upgrade requires state migration."
  ],
  "nonGoals": [
    "Do not add new pages or routes beyond the existing minimal 3-page site (Home, Learn More, Special Event).",
    "Do not introduce new authentication methods or third-party integrations.",
    "Do not change site content/copy or design as part of this request; only publish the current draft to production."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}